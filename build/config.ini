
[common]

log = RtpAudioCapture.log ; 日志文件名，为空时不写日志

eth = 38:FC:98:8A:24:01 ; 指定使用哪个网卡，填ip或mac地址

rtp_port = 3001 ; 直接透传rtp数据的目的端口
timeout = 20 ; 某个端口的流的超时时间，单位秒。当超时时，把之前的数据保存成一个文件；之后有数据到来保存成另一个文件

amr = 102,103,104,105,108
amrwb = 106,107

